<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MFApp.Views.NewPlayerPage"
             Title="Neuer Spieler">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Abbrechen" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Speichern" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Initialen" FontSize="Medium" />
            <Entry Text="{Binding Player.Initials}" d:Text="Item name" FontSize="Small" />
            <Label Text="Name" FontSize="Medium" />
            <Editor Text="{Binding Player.Name}" d:Text="Name" FontSize="Small" Margin="0" />
            <Label Text="Handicap" FontSize="Medium" />
            <Editor Text="{Binding Player.Handicap}" d:Text="Handicap" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
