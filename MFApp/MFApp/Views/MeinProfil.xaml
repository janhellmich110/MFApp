<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MFApp.Views.MeinProfil">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Spacing="20" Padding="15">
                <Label Text="Initialen:" FontSize="Medium" />
                <Label Text="{Binding Initials}" d:Text="Initialen" FontSize="Small "/>
                <Label Text="Name:" FontSize="Medium" />
                <Label Text="{Binding Name}" d:Text="Name" FontSize="Small"/>
                <Label Text="Handicap:" FontSize="Medium" />
                <Label Text="{Binding Handicap}" d:Text="Handicap" FontSize="Small"/>
                <Label Text="Birthday:" FontSize="Medium" />
                <Label Text="{Binding Birthday, StringFormat='{0:d}'}" d:Text="Birthday" FontSize="Small" />
            </StackLayout>
            <StackLayout Spacing="20" Padding="15" VerticalOptions="CenterAndExpand">
                <Button Text="Abmelden"
                        Clicked="Logout_Clicked"
                    VerticalOptions="Center" 
                    HorizontalOptions="CenterAndExpand"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>